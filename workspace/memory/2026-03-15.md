# 2026-03-15

## Priority Extracts (Nightly Learn)
- [P1] tasks.json can become corrupted into a non-JSON single-line path; restore from git (known good commit) then rerun validate-tasks-integrity.ps1.
- [P1] Heartbeat recentErrors burst was largely caused by malformed PowerShell one-liners (missing string terminator / invalid assignment) and non-elevated kill attempts; should avoid heavy quoting and prefer file-based args or Python.
- [P2] Mission Control (port 8765) restart via ScheduledTask may fail silently; validate with Test-NetConnection and netstat, and avoid claiming running without receipt.
- [P2] Elevated process kill is not available in runtime=direct; automation must degrade gracefully (don't loop on Access Denied).
- [P2] Use utf-8-sig when reading tasks.json from git restoration to handle BOM.
