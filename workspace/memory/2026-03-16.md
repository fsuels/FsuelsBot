# 2026-03-16

## Notes

- Created scripts/nonstop-guard.ps1 (watchdog) because watchdog referenced missing file.
- Observed clawdhub update --all failing on Windows with relative URL parse error; explore/search work.
- Verified installed clawdbot matches npm latest (2026.1.24-3).
- PowerShell one-liners containing `$var=...` appear to lose `$var` before execution in this runtime (cmd becomes `=1`), causing many `.Name` / `=` errors. Avoid `$` in inline PowerShell; prefer file-based scripts, `Set-Variable`, or Python.
